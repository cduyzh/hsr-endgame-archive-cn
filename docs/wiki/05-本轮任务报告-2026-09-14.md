# 本轮任务报告（2026-09-14 批次 1–3 全量执行）

> 用户授权「代跑 + 按顺序全部执行完三个批次」，因此本轮的**生产灌库、DDL 迁移与 Netlify 发布都由本会话执行**，不再只交付语句清单。
> 缺陷逐条的修法与取证在 [04-生产验证缺陷清单](./04-生产验证缺陷清单.md)，本页是任务台账：做了什么、怎么验的、还有什么没做。

## 结论一览

| 批次 | 内容 | 状态 | 线上/仓库证据 |
| --- | --- | --- | --- |
| 1 | 当期新单位数据 + 名字清洗 + 审核台 D2/D3 | ✅ 已上线 | 线上 `units` 266 → 268（真珠、献给明日的色彩）、全表无 `<unbreak>`；`?status=withdrawn` 由 400 变 200 |
| 2 | D13 发布链路根治 | ✅ 已上线 | 新脚本连用两次真实发布，`deploy exit=0`，线上包 `index-BOFZn9Cn.js` 与本地 `dist` 同名 |
| 3 | D5/D12 纵深 + D4/D6/D7/D10 清账 | ✅ 已上线 | 线上直 POST 脏 payload → `400` 且队列无变化；生产库两条 CHECK 约束实测拒绝脏值 |
| — | 文档同步契约 + 提交 + 推送 | ✅ 完成 | `09c97e0`（代码 + 文档）、`d6f075a`（补记生产实测），`origin/main = d6f075a` |

构建门禁：`pnpm build`（typecheck + vitest + lint + vite 并行）exit 0，**26 个测试文件 / 208 个用例**，开工时是 22 / 182。
注意：本机默认 `node` 是 v16，`vitest` 会以 `stripVTControlCharacters` 报错直接崩，必须走 nvm 的 v24（`.nvmrc` 已固定）。

## 我在生产执行的写操作

1. **灌库**：`pnpm seed:archive -- --table=characters,lightcones` → 98 角色 + 170 光锥 upsert。刻意限定表，不碰 `seasons` / `articles` / `stages`。
   - 连接串取自 Netlify 生产环境变量（`netlify env:get NETLIFY_DATABASE_URL`），全程未回显、用完即删临时文件。
2. **DDL 迁移**：执行 `netlify/migrations/2026-09-14_runs_status_and_mode_check.sql`，给 `runs.status` / `runs.mode` 加 CHECK。
   - 执行前先核对存量（`status` 只有 `approved`/`rejected`、`mode` 只有 `aa`、原有 CHECK 0 条）；执行后查 `pg_constraint` 确认两条都在。
   - 负向验证用「违规 UPDATE + `select 1/(select 0)`」的**必回滚事务**，两条约束都报 `violates check constraint`，探针行原值未变。
3. **两次 Netlify 生产发布**：`6aa8a86c…`（批次 1）、`6aa8b23f…`（批次 3）。
4. **一次完整审核往返**（验证本地跑不到的有库事务分支）：入队 `202` → 通过 `200` → 公开列表可见 → 改判驳回 → 作者硬删 → 二次删 `404`。
   - 零残留判据按既有纪律：三张表的 **id 集合与计数回到基线**（3 / 3 / 24 / 28），`run_units` 无孤儿行；`stages` 仍 28 行，证明**没有再造占位行**。
   - 探针记录选的是**已存在于 `stages` 的阶段**（`4.5-aa-checkmate`），所以不会新增阶段行；凭证在入队后第一件事就落盘，再做后续写动作。

## 验证档位（哪些是实测、哪些只是静态过）

- **已实测（生产）**：`?status=withdrawn` 200、脏 payload 400 且不入队、线上包哈希与 `0.9.2` 字样、真珠与专武在 `/api/archive/config` 里、角色名无标记、CHECK 约束拒绝脏值、审核通过事务分支往返。
- **已实测（本地单测）**：审核卡片四档状态与按钮矩阵、`serverNotes` 基线行为、`checkSubmissionRules` 九条判定、`deploy-netlify.sh` 的 CLI 复用分支。
- **仅静态审查 / 未实测**：审核写入失败时的 `502` 回滚路径（要造一次真实数据库故障才会触发）、`bossId` 既不在库也不在快照时的 `400` 分支（现役投稿取的都是快照内阶段）。
- **未覆盖**：`archive-config` 里那条无人消费的 `articles` 查询（见下）。

## 本轮查出并当场修掉的两个「不在清单里」的问题

- **`sync:units` 读的是赛季目录**（登记为 D14）。这才是「修好了却没上线」之外第二个静默失效点：脚本写死 `hsr/4.5/`，而新单位只出现在最新数据目录 `4.5.54/`，所以就算有人跑了 `sync:units` 也拿不到当期角色。现改为跟随 manifest，与运行时同口径。
- **`<unbreak>` 排版标记进库**（登记为 D15）。怪物脚本有清洗、角色脚本没有，脏名字已在生产 `characters` 表里；实测时线上 approved `runs` 为 0 条，属于「还没有玩家看到」的窗口期问题。

D4 的测试还当场抓出一个我自己写的 bug：`notes` 与 `serverNotes` 最初共用同一个对象引用，管理员一改备注，基线跟着变，新写的备注会被当成旧值丢掉——已改为存拷贝。

## 遗留（不在本轮三个批次范围内）

1. **`archive-config.ts` 仍查 `articles` 表**：每次 `/api/archive/config` 多一条无用库查询，界面已完全不消费。删字段 / 删查询 / `DROP TABLE` 需单独决策。
2. **库里的历史遗留结构**（`hsr_shared`、`archive_stage_sources`、`characters.source_id` / `recommended_lightcone_ids`）：归属已查实为**本项目 2026-07-22 早期迭代的遗留**（迁移文件从未进库、表全 0 行、推荐光锥特性已被 `lightcone-pairs.json` 取代），处置是保持不动；要收口先把那两个迁移补进仓库留痕再决定删留。细节见 [04-生产验证缺陷清单](./04-生产验证缺陷清单.md)。
3. **4.6 新赛季**：上游 `manifest.hsr.live` 仍是 `4.5`，`STATIC_SEASON_IDS` 无需改动；属于等上游的观察项。
4. **首页首屏垂直预算**：速报高度随文章浮动，当前只剩 `PromoSlot` 可压，本轮未动。

## 复跑方法

```bash
export PATH=~/.nvm/versions/node/v24.18.0/bin:$PATH   # 默认 node 是 v16，vitest 会崩
pnpm build                                             # 期望 26 文件 / 208 用例
curl -s https://hsr-archive.cduyzh.top/api/archive/config | grep -c 真珠
curl -s -o /dev/null -w "%{http_code}\n" -X POST https://hsr-archive.cduyzh.top/api/submissions \
  -H 'content-type: application/json' \
  -d '{"seasonId":"4.5","mode":"bogus","bossId":"4.5-moc-top","category":"asScore4000","author":"x","teamName":"x","cycle":1,"score":1,"cost":999,"videoUrl":"https://example.com/x","notes":"","flags":[],"units":[{"unitId":"a","eidolon":0}],"lightcones":[]}'   # 期望 400
# 审核台筛选需要鉴权：Authorization: Bearer <ADMIN_REVIEW_PASSWORD>
```
